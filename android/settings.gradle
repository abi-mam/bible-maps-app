pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    // ✅ Use PREFER_PROJECT so Gradle doesn't ignore repositories in build.gradle
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    }
}

// ✅ Ensure your app module is included
rootProject.name = "BibleMaps"
include(":app")
